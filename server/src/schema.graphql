# import * from './generated/prisma.graphql'

type Query {
  posts: [Post!]!
}

type Mutation {
  # post
  createDraft(draft: PostContent): Post
  publish(id: ID!): Post
  delete(id: ID!): Post
}

input PostContent {
  title: String!
  author: String!
  content: String!
  savedOn: DateTime
}
