type User {
  id: ID! @id
  name: String!
}

type Post {
  id: ID! @id
  title: String!
  author: String!
  content: String!
  savedOn: DateTime
  publishedOn: DateTime
  isPublished: Boolean @default(value: false)
}