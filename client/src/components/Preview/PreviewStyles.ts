import { css } from '@emotion/core';

export const button = css`
  width: fit-content;
  padding: 8px 16px;
  border: 1px solid var(--primary);
  border-radius: 5px;
  font-size: 14px;
  font-weight: 500;
  color: var(--primary);
  label: btn--publish;
`;